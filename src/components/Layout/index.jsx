import { Grid } from "./styles";

import Content from "../Content";
import Header from "../Header";

const Layout = () => {
  return (
    <Grid>
      <Header />
      <Content />
    </Grid>
  );
};

export default Layout;
